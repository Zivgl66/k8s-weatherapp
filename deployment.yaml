apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-app
  labels:
    app: weather-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: weather-app
  template:
    metadata:
      labels:
        app: weather-app
    spec:
      containers:
        - name: weather-app
          image: zivgl66/weatherapp:6b53befceb7e1f30a2242f70623e1dc40acc9482
          ports:
            - containerPort: 5000
          env:
            - name: BG_COLOR
              valueFrom:
                configMapKeyRef:
                  name: env-config-green # Name of your ConfigMap
                  key: BG_COLOR # Key within the ConfigMap
      imagePullSecrets:
        - name: docker-hub-cred
